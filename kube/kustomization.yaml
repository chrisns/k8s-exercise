apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

resources:
  - ingress.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml

namespace: default
